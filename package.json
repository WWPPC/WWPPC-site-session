{
    "name": "wwppc-session",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "deploy": "npm run build && firebase deploy --only hosting:session-wwppc"
    },
    "devDependencies": {
        "typescript": "^5.8.3",
        "vite": "^6.2.5"
    }
}
